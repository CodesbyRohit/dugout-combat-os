{
  "name": "dugout-root",
  "version": "1.0.0",
  "description": "Experimental multi-agent cricket intelligence platform",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "server": "npm run start --prefix server",
    "client": "npm run dev --prefix client"
  }
}
